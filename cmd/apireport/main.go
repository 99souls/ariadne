//go:build ignore

// Legacy placeholder: the API report tool has moved to `tools/apireport`.
// This stub remains temporarily to avoid stale references in external docs or scripts.
// It is excluded from builds via the ignore build tag and will be removed in a future cleanup.
package main

func main() {}
